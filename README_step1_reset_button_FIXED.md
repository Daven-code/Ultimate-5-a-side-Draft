# Step 1 - Reset button visibility FIXED

This version makes the reset button hidden in the HTML immediately, before JavaScript runs.

Change made:
- `Reset game` is hidden on the main/front screen from first page load.
- It is shown when a game starts.
- It is hidden again when resetting back to the front screen.

No game logic has been changed.
